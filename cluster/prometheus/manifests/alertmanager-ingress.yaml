apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: alertmanager-ingress
  namespace: monitoring
spec:
  rules:
  - host: alertmanager.local.io
    http:
      paths:
      - backend:
          serviceName: alertmanager-main
          servicePort: web